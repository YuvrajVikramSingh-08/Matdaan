import React from 'react';

/** Placeholder for Google Maps — rendered when API key is available */
export default function BoothMap() {
  return (
    <div className="booth-map" aria-label="Map showing nearest polling booths">
      <p>Map loading...</p>
    </div>
  );
}
